/// collideSolidTerrain(x, y)

var inst = instance_place(argument0, argument1, obj_surface_parent);
if (inst == noone || inst.object_index == obj_plat_small)
    return false;
    
return true;


